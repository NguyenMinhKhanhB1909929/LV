export const testSelector = (state) => state.test.dem;
export const authSelector = (state) => state.auth;
export const jobSelector = (state) => state.job;
export const offerSelector = (state) => state.offer;
